import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  container: {
    flex: 1,
    padding: 8,
    paddingTop: 20,
    justifyContent: 'center',
  },
  button: {
    backgroundColor: '#ccc',
    paddingVertical: 8,
    alignItems: 'center',
    marginVertical: 4,
  }
});